{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "lint": "eslint .",
    "test": "jest"
  },
  "dependencies": {
    "@lobby/shared": "workspace:*"
  }
}
